package SalesManager;

import Abstract.playerInformation;
import gameShop.Players;

@SuppressWarnings("unused")
public class sales extends playerInformation implements ISales {

	public sales satis1(sales Sales) {
		
		System.out.println(playeradd1(null).getFirstName() + "Adlı oyuncu" + gameAdd1(null).getName() + "oyunu satın aldı");
		
		
		;
		return Sales;
		
		
	}
	
}
